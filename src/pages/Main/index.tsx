import MainBannerContainer from 'containers/Main/Banner';
import * as Styled from './styled';

const Main = () => {
  return (
    <Styled.Root>
      <MainBannerContainer />
    </Styled.Root>
  );
};

export default Main;
